package com.mobile.rick_and_morty.ui.designsystem.grid

import androidx.compose.foundation.shape.RoundedCornerShape

object RickMortyShapes {
        val small = RoundedCornerShape(Sizes.size12)
        val medium = RoundedCornerShape(Sizes.size16)
        val large = RoundedCornerShape(Sizes.size20)
}